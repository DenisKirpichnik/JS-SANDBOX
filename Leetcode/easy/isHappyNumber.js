/**
 * @param {number} n
 * @return {boolean}
 */
var isHappy = function (n) {
  const numToStr = n.toString();
  const arr = numToStr.split("");
  for (let i = 0; i < arr.length; i++) {
    let sum = 0;
    sum += arr[i];
  }
};
